---
description: Sample command with legacy memory section (no markers)
allowed-tools:
  - Bash
  - Read
---

# Sample Command

This command has a legacy memory section without markers.

## Memory Operations

Before implementing, search for prior context:
```bash
rg -i "relevant" ~/.claude/mnemonic/ --glob "*.memory.md"
```

After completing work, capture discoveries:
```bash
/mnemonic:capture decisions "Decision title"
```

## Procedure

1. Do something
2. Do something else
